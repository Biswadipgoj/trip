---
description: Detect and install the five engineering integrations into this project
argument-hint: [--pinned | --with-tools | --offline | --only <id>]
---

Read `.claude/skills/biswodip-bootstrap/SKILL.md` and follow it.

Detect before installing; never reinstall what is present; never duplicate a skill that exists in another scope; verify every checkout; record the result in `.biswodip/integrations.lock.json`.

Extra flags for this run: $ARGUMENTS

Finish with the table: `INTEGRATION | STATUS | VERSION | SOURCE | LOCATION | ACTION TAKEN`, and name anything `BLOCKED` with its reason.
